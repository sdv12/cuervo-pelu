# Supabase — copiá estos valores desde Project Settings > API en supabase.com
# Si se dejan vacíos, el sitio funciona con datos de ejemplo en memoria.
# Con las dos cargadas, se activan el login, los paneles y la reserva real.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# WhatsApp de la peluquería (código país + área + número, sin + ni espacios)
VITE_WHATSAPP_NUMBER=5491100000000
